<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.tattobr.gametests">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ColoredScreenGame" />
        <activity android:name=".activities.CannonRotationGame" />
        <activity android:name=".activities.CannonGravityGame" />
        <activity android:name=".activities.CannonCollisionGame" />
        <activity android:name=".activities.CannonCollisionCameraGame" />
    </application>

</manifest>
